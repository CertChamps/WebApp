/**
 * Optional overrides for which image topic is free per Storage folder name
 * (e.g. "english", "irish"). Value = topic folder name under the level.
 * When absent, the first topic alphabetically is the free sample.
 */
export const FREE_IMAGE_TOPIC_BY_STORAGE_FOLDER: Record<string, string> = {};
