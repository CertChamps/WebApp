# Local-only Spotify redirect (Vite loads this in `npm run dev` only).
# Spotify rejects "localhost" — use 127.0.0.1 for loopback.
# After Connect Spotify you may land on 127.0.0.1; that's expected.
VITE_SPOTIFY_REDIRECT_URI=http://127.0.0.1:5173/callback
